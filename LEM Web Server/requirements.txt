# LEM Web Server — runtime dependencies, exactly pinned.
#
# This is what the updater builds each release venv from. Pins are ``==``, not
# bare names, on purpose: a deploy must install exactly what was tested. This
# file previously listed four packages with no versions at all, so two deploys
# a week apart could install different code from identical source — and the
# automatic rollback would blame the commit rather than the dependency.
#
# Versions captured from the live .venv-win on ASAPSV1 on 2026-08-21 — the set
# the server was actually running against. Note these are NOT the same versions
# COA Reviewer pins; the two apps have separate venvs and there is no reason to
# couple them.
#
# pytest moved to requirements-dev.txt: a release venv should not carry the
# test runner.

# ── direct ──────────────────────────────────────────────────────────────────
Flask==3.1.3
requests==2.34.2
pystray==0.19.5
pillow==12.3.0

# ── transitive ──────────────────────────────────────────────────────────────
# Pinned too. These are exactly the packages that break a deploy without anyone
# having touched them — Werkzeug in particular ships breaking changes that
# surface only at Flask import time.
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6          # click pulls this on Windows only
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
six==1.17.0
urllib3==2.7.0
Werkzeug==3.1.8
